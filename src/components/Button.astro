---
const {href} = Astro.props
const Judge = href ? 'a' : 'button'

---

<Judge href={href} class="b1" target="_blank">
  <slot/ >
</Judge>

<style>
	
    .b1 {
        background-color: #ffffff;
        border:  3px solid #a6bad2; 
        padding: 0rem 1rem;
        margin: 0.5rem 0.5rem;
        /* font-size:0.875rem;
        text-decoration: none; */
        display: inline-block;
        cursor: pointer;
        border-radius: 30px;
        /* box-shadow: 0 0px 3px 0 rgba(157, 206, 167, 0.156), 0 6px 20px 0 rgba(50, 149, 68, 0.189); */
    }

    #g{
      margin: 0.5rem 0.5rem;
		  position: absolute;
  		top: 0.5rem;
 		  right: 1rem;
    }

    .b1:hover{
      box-shadow: 0 3px 5px 0 rgba(48, 45, 45, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
    }

    @media screen and (max-width: 576px) {
		.b1 {
			/* background: rgb(60, 60, 31); */
			display: grid;
      margin: 1rem 2rem;
		}
		
	}
  </style>
